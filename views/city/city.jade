extends ../layout

block content
    h2= city.name
    ul
        for post in posts
            li
                a(href='/city/#{city.slug}/post/#{post.id}')= post.title
        li
            a(href='/city/#{city.slug}/post/new') Create new Post

